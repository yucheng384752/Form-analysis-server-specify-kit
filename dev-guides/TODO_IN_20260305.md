# TODO_IN_20260305

## Priority Definition
- `P0`: Blocks demo/use flow or produces wrong decision output.
- `P1`: Core analysis quality and traceability.
- `P2`: UX and observability improvements.

## P0
- [x] `P0-1` Product input normalization and hit-rate uplift (cross-complaint product_ids)
  - Scope:
    - Normalize user-entered `product_id` before artifact matching.
    - Return both original and normalized candidates to frontend/API caller.
    - Keep backward compatibility of existing `requested/resolved/unmatched/matches`.
  - Deliverables:
    - API response adds:
      - `normalized_inputs: { requested_id: [candidate...] }`
      - `match_diagnostics: { requested_id: { matched_by: [candidate...], candidate_count: n } }`
    - Matching process uses normalized candidates first, then trace tokens fallback.
  - Acceptance:
    - `TC-P0-01`: Same logical ID with `-` / `_` variants can hit the same artifact rows.
    - `TC-P0-02`: Unmatched IDs return diagnostics without 500.
    - `TC-P0-03`: Existing consumers reading old fields still work.
  - Progress (2026-03-05):
    - Done: backend adds `normalized_inputs` + `match_diagnostics`, and unmatched reason classification (`invalid_format`, `no_trace`, `artifact_no_data`).
    - Done: frontend improvement page now shows input hit summary and unmatched diagnostics.
    - Done: backend automated tests added for classification and normalized hit behavior.
  - Follow-up (optional hardening):
    - Add service-level unit tests for candidate normalization edge cases (non-ASCII, whitespace-only separators, long mixed tokens).
    - Add frontend i18n labels for `reason_code` display (currently plain code + message).

- [x] `P0-2` Resolve-input observability baseline
  - Scope:
    - Add lightweight timing visibility for `resolve-input`.
    - Keep response backward compatible.
  - Deliverables:
    - API response adds `elapsed_ms`.
    - API response adds summary metrics:
      - `requested_count/resolved_count/unmatched_count`
      - `trace_attempted_count/trace_resolved_count`
      - `unmatched_reason_counts`
    - Smoke/API tests assert observability fields exist and are numeric/consistent.
  - Acceptance:
    - `TC-P0-04`: `resolve-input` returns `elapsed_ms` for matched and unmatched cases.
    - `TC-P0-05`: `resolve-input` returns summary counts and reason aggregation.
  - Progress (2026-03-05):
    - Done: backend response schema and implementation updated with `elapsed_ms`.
    - Done: count summary fields and trace summary fields added.
    - Done: unmatched reason aggregation field added.
    - Done: backend API tests updated and passing.
  - Follow-up (optional hardening):
    - Display `trace_attempted_count/trace_resolved_count/unmatched_reason_counts` in frontend report header.

## P1
- [x] `P1-1` Mapping transparency (`product_id -> event_id/Produce_No`)
  - Scope:
    - Show deterministic mapping table for each requested `product_id`.
  - Acceptance:
    - `TC-P1-01`: User can see which event/token each `product_id` matched.
    - `TC-P1-02`: Unmatched reason category is returned.
  - Progress (2026-03-05):
    - Done: frontend improvement section renders mapping table (`product_id / hit_count / matched_tokens / reason`).
    - Done: observability summary row includes trace and unmatched reason aggregation.
    - Done: mapping row supports direct navigation (`關聯查詢`) to matched detail row/event when token is resolvable in current view.
    - Done: explicit fallback hint is shown when token cannot map to current visible list (cross-view mismatch).
  - Next:
    - Optional hardening: add per-view deep-link strategy (e.g. auto-switch to events/llm when token type clearly indicates target view).

- [x] `P1-2` Cross-complaint aggregate layer
  - Scope:
    - Aggregate multiple complaint product_ids into one analysis snapshot.
    - Output station/machine summary and top features.
  - Acceptance:
    - `TC-P1-03`: Aggregated totals equal detail totals.
    - `TC-P1-04`: Snapshot can be reused by chart + LLM report.
  - Progress (2026-03-05):
    - Done: frontend improvement section adds a cross-complaint snapshot block.
    - Done: backend unified snapshot endpoint added:
      - `GET /api/v2/analytics/artifacts/{artifact_key}/snapshot?product_ids=...`
      - unified schema: `station_distribution/machine_distribution/top_features/metrics`
    - Done: backend API test added for snapshot endpoint.
    - Done: frontend switched to consume unified snapshot endpoint, with local aggregation fallback.
    - Done: backend unified snapshot now enriches `llm_reports` / `rag_results` with station distribution and feature counters from raw artifact payloads.
    - Done: service-level tests added for unified snapshot llm/rag aggregation.
  - Next:
    - Optional hardening: extend machine extraction for llm/rag when raw payload includes machine-like fields with variant keys.

## P2
- [ ] `P2-1` Machine cumulative NG bar chart in complaint improvement flow
  - Scope:
    - Add chart in improvement section (product_id entry path), close to LLM report.
  - Acceptance:
    - `TC-P2-01`: Chart updates after changing selected complaint set.
    - `TC-P2-02`: Click machine filters detail list.
  - Progress (2026-03-08):
    - Done: NG drill-down query path in `AnalyticsPage` now uses backend dynamic query with explicit P2 rule:
      - `data_type=P2`
      - `winder_number=clicked bar value`
      - `row_data.Striped Results eq 0`
    - Done: frontend NG判定同步收斂為「只認 Striped Results = 0」。
    - Done: frontend tests pass (`26 passed, 1 skipped`).
    - Done: backend regression test added and passing:
      - `test_v2_query_dynamic_p2_winder_and_striped_results_eq_zero`
      - verifies `row_data.Striped Results eq 0` works for both numeric `0` and string `"0"` with `winder_number` filter.
  - Next:
    - Run one demo UI smoke evidence: click winder bar -> dynamic payload contains `row_data.Striped Results eq 0` -> list returns only NG rows.

- [ ] `P2-2` LLM report alignment with same filtered dataset
  - Scope:
    - Ensure chart and LLM use same event scope.
  - Acceptance:
    - `TC-P2-03`: Chart counts and LLM totals are consistent.

- [ ] `P2-3` Unmatched UX and operator guidance
  - Scope:
    - Classify unmatched reasons and provide actionable guidance.
  - Acceptance:
    - `TC-P2-04`: Unmatched rows include reason code.

- [ ] `P2-4` Rate-limit and batch-query robustness
  - Scope:
    - Batch lookup and reduce request bursts on multi-product analysis.
  - Acceptance:
    - `TC-P2-05`: 20 product_ids can be processed in one run without UI hard fail.

- [ ] `P2-5` Decision-oriented layout for improvement page
  - Scope:
    - Reorder sections: hit overview -> machine NG bar -> station pareto -> LLM -> detail.
  - Acceptance:
    - `TC-P2-06`: Operator can identify top machine in <=3 interactions.

- [ ] `P2-6` Quality metrics baseline
  - Scope:
    - Add measurable KPIs (hit rate, consistency rate, analysis latency).
  - Acceptance:
    - `TC-P2-07`: KPI values are available in smoke result doc.

## Complaint Input Pipeline Backlog (product_id as primary entry)

### P0 (must-have, single-flow foundation)

- [x] `P0-3` Single orchestrator endpoint for complaint analysis
  - Scope:
    - Add one backend orchestration endpoint that runs:
      1) resolve-input
      2) unified snapshot
      3) report assembly payload
    - Keep existing endpoints as internal building blocks.
  - Deliverables:
    - New endpoint: `POST /api/v2/analytics/complaint-analysis`
    - Response includes:
      - `input_summary` (requested/resolved/unmatched counts)
      - `mapping` (`requested_id -> matched_tokens/reason_code`)
      - `snapshot` (`station_distribution/machine_distribution/top_features/metrics`)
      - `report_payload` (llm/rag consumable section)
  - Acceptance:
    - `TC-P0-08`: One API call returns complete complaint analysis payload.
    - `TC-P0-09`: Existing `resolve-input` and `snapshot` behavior remains backward compatible.
  - Progress (2026-03-08):
    - Done: backend endpoint `POST /api/v2/analytics/complaint-analysis` added.
    - Done: endpoint orchestrates `resolve-input -> unified snapshot -> report payload`.
    - Done: response includes `analysis_scope_id`, `input_summary`, `mapping`, `snapshot`, `report_payload`, `elapsed_ms`.
    - Done: backend API test added and passing (`test_v2_analytics_complaint_analysis_ok`).
    - Done: frontend complaint input flow switched to call the new orchestrator endpoint (`ArtifactsView`).
    - Done: frontend still uses resolved tokens for current artifact list rendering (behavior compatibility kept).
    - Done: frontend tests passing (`26 passed, 1 skipped`).
  - Next:
    - Promote orchestrator payload usage for report block rendering (reduce duplicate list/detail fetches).

- [x] `P0-4` Scope lock and consistency guard
  - Scope:
    - Ensure all downstream chart/report blocks use identical resolved token scope.
  - Deliverables:
    - Add `analysis_scope_id` (or stable hash from resolved tokens) in response.
    - Attach `analysis_scope_id` to snapshot and report sections.
  - Acceptance:
    - `TC-P0-10`: Chart totals and report totals are computed from the same scope.
    - `TC-P0-11`: Partial unmatched input does not alter matched scope calculations.
  - Progress (2026-03-08):
    - Done: complaint-analysis response adds `scope_tokens_count` and `consistency` block.
    - Done: backend marks `snapshot_scope_locked/report_scope_locked` and returns per-section sample counts.
    - Done: frontend displays `scope_id/scope_tokens/scope_lock` in complaint summary pills.
    - Done: backend/frontend tests passing after change.
  - Next:
    - Add one end-to-end smoke case validating scope lock values and counts in UI.

- [x] `P0-5` Unmatched fallback policy
  - Scope:
    - Define deterministic behavior for all-unmatched / partial-unmatched cases.
  - Deliverables:
    - `all unmatched`: return diagnostics only, no chart/report render data.
    - `partial unmatched`: return chart/report for matched scope + unmatched warning block.
  - Acceptance:
    - `TC-P0-12`: All-unmatched request returns 200 with diagnostics-only payload (no 500).
    - `TC-P0-13`: Partial-unmatched request renders usable analysis with explicit warnings.
  - Progress (2026-03-08):
    - Done: backend complaint-analysis now returns diagnostics-only payload when `resolved_tokens` is empty.
    - Done: in all-unmatched flow, snapshot/report are not built from full dataset (prevents misleading charts).
    - Done: backend API test added and passing (`test_v2_analytics_complaint_analysis_all_unmatched_returns_diagnostics_only`).
    - Done: frontend adds explicit diagnostics-only notice when `requested>0 && resolved=0`.
    - Done: smoke docs updated with `TC13` for complaint-analysis all-unmatched fallback.
  - Next:
    - Optional: add UI component-level test for diagnostics-only notice rendering.

### P1 (analysis quality and traceability)

- [x] `P1-3` Complaint-first frontend flow (one-click run)
  - Scope:
    - Use current complaint product_id input as primary analysis trigger.
    - Remove dependency on date-first path for complaint workflow.
  - Deliverables:
    - Frontend button/action: `開始客訴分析`
    - Single request to orchestrator endpoint.
    - Render order:
      1) input hit summary
      2) machine/station/features snapshot
      3) improvement report
      4) trace/mapping detail
  - Acceptance:
    - `TC-P1-05`: User can complete complaint analysis without entering date range.
    - `TC-P1-06`: 20 product_ids can be processed in one run and shown in one page.
  - Progress (2026-03-08):
    - Done: frontend adds explicit action button `開始客訴分析` for complaint-mode execution.
    - Done: when complaint product_ids are present, flow is manual-run (avoids repeated auto-query while editing input).
    - Done: retains existing re-load behavior and compatibility with current artifact list/detail rendering.
    - Done: frontend tests passing after one-click flow update.
  - Next:
    - Optional: add one UI test for pending state hint (`已更新客訴輸入，請按「開始客訴分析」執行。`).

- [x] `P1-4` Mapping and diagnostics transparency upgrade
  - Scope:
    - Expand existing mapping table with deterministic reason and stage labels.
  - Deliverables:
    - Mapping row fields:
      - `requested_id`, `normalized_candidates`, `resolved_tokens`, `reason_code`, `reason_message`
    - Stage marker:
      - `matched_direct`, `matched_trace`, `unmatched`
  - Acceptance:
    - `TC-P1-07`: Every unmatched row has `reason_code` and `reason_message`.
    - `TC-P1-08`: Operator can identify whether each hit is direct or via trace.
  - Progress (2026-03-08):
    - Done: frontend mapping table now reads `complaint-analysis.mapping` and displays `matched_stage`.
    - Done: `matched_tokens` now prefer backend mapping tokens (instead of only local matched_by hints).
    - Done: mapping action (`關聯查詢`) now uses `matched_tokens` for detail jump.
    - Done: frontend tests passing after change.
  - Next:
    - Optional: add dedicated UI test assertion for `matched_stage` values (`matched_direct/matched_trace/unmatched`).

- [x] `P1-5` Report composition standardization
  - Scope:
    - Normalize report payload schema so LLM/RAG block is stable across views.
  - Deliverables:
    - `report_payload` fixed schema:
      - `summary`
      - `suggestions`
      - `evidence_refs` (event/token references)
  - Acceptance:
    - `TC-P1-09`: Report section renders without per-artifact custom parsing branches.
    - `TC-P1-10`: Evidence refs can map back to visible detail rows.
  - Progress (2026-03-08):
    - Done: backend complaint-analysis response adds `report_composition` with fixed schema:
      - `summary[]`
      - `suggestions[]`
      - `evidence_refs[]`
    - Done: frontend improvement section now renders standardized report block from `report_composition`.
    - Done: backend API tests and frontend tests are passing after this change.
    - Done: complaint-analysis now builds DB-based unified source scope from traceability (`P1/P2/P3`) and returns:
      - `source_scope` (`p1_count/p2_count/p3_count/total_records`)
      - `report_input_scope` (`mode=db_unified_p1p2p3`, requested/resolved/scope counts)
    - Done: `report_composition` summary/evidence now includes DB unified scope signals.
  - Next:
    - Tighten `evidence_refs` deep-link behavior to always jump to visible detail row across views.

### P2 (UX and observability hardening)

- [x] `P2-8` Frontend state model cleanup for complaint mode
  - Scope:
    - Separate complaint-mode state from date-mode state to avoid cross-over bugs.
  - Deliverables:
    - Isolated state slices:
      - `complaint_input_state`
      - `complaint_result_state`
      - `date_analysis_state`
  - Acceptance:
    - `TC-P2-08`: Clearing complaint input clears complaint outputs only.
    - `TC-P2-09`: Date-mode chart actions do not overwrite complaint results.
  - Progress (2026-03-08):
    - Done: date clear action no longer clears complaint(product_id) state.
    - Done: complaint-mode has explicit manual run trigger, reducing cross-over updates while editing inputs.
    - Done: frontend tests passing after state-isolation change.
  - Next:
    - Add a focused UI smoke check for `clear date` and `clear product_id` interaction sequence.

- [x] `P2-9` Observability extension for complaint pipeline
  - Scope:
    - Add timing and count breakdown for each orchestrator stage.
  - Deliverables:
    - Response adds:
      - `timing.resolve_ms`
      - `timing.snapshot_ms`
      - `timing.report_ms`
      - `timing.total_ms`
    - Keep existing `elapsed_ms` compatible where applicable.
  - Acceptance:
    - `TC-P2-10`: Stage timing fields are always present and numeric.
    - `TC-P2-11`: Smoke result doc can compare stage latency across runs.
  - Progress (2026-03-08):
    - Done: backend complaint-analysis response adds `timing`:
      - `resolve_ms`
      - `snapshot_ms`
      - `report_ms`
      - `total_ms`
    - Done: API tests assert timing fields are present and numeric for both matched and all-unmatched flows.
    - Done: frontend diagnostics panel now shows stage timing pills (`t.resolve/t.snapshot/t.report/t.total`).
  - Next:
    - Add smoke record entry with one real run of stage timing numbers.

- [x] `P2-10` Complaint-mode smoke pack
  - Scope:
    - Add dedicated smoke cases for complaint entry (without date path).
  - Deliverables:
    - Update smoke docs with:
      - full hit
      - partial hit
      - all unmatched
      - mixed format normalization
      - cross-artifact consistency check
  - Acceptance:
    - `TC-P2-12`: Complaint smoke pack passes in demo environment.
    - `TC-P2-13`: Summary fields and diagnostics fields are all validated in results doc.
  - Progress (2026-03-08):
    - Done: `SMOKE_TEST_DEMO.md` now includes complaint-mode dedicated smoke cases (`TC-P2-12-01~05`, `TC-P2-13-01~03`).
    - Done: `SMOKE_TEST_DEMO_RESULT_20260223.md` now includes complaint-mode execution/result template.
  - Next:
    - Keep this section as baseline and rerun when complaint flow fields change.

## P0/P1 Addendum (P2 lot 7+2 + trace 7+2+2)

- [x] `P0-6` Add P2 trace token field (`trace_lot_no`, 7+2+2)
  - Scope:
    - Keep `lot_no` semantics as `7+2`.
    - Add a separate deterministic token field for trace/artifact linking.
  - Deliverables:
    - DB column on `p2_items_v2`: `trace_lot_no` (nullable during rollout).
    - Index: `(tenant_id, trace_lot_no)`.
    - Import path writes `trace_lot_no = {lot_no_norm}_{winder_number:02d}`.
  - Acceptance:
    - `TC-P0-14`: New P2 imports persist `trace_lot_no` in `NNNNNNN_NN_NN` format.
    - `TC-P0-15`: Existing query paths remain backward compatible (no API contract break).
  - Progress (2026-03-08):
    - Done: model adds `p2_items_v2.trace_lot_no` with tenant+token index.
    - Done: alembic migration added (`20260308_0002`) for column + index.
    - Done: P2 import commit path now writes `trace_lot_no = {lot_no_norm}_{winder:02d}`.
    - Done: API test confirms import merge path persists `trace_lot_no` (`test_commit_import_job_p2_business_key_merge`).
  - Remaining test records:
    - Not done: `TC-P0-15` cross-check on demo Postgres DB after full migration + legacy query paths smoke (local env currently blocked by DB not running at `localhost:5432`).
  - Follow-up solutions:
    - Start demo DB, run migration, then execute `SMOKE_TEST_DEMO.md` query-path subset and append result evidence.

- [x] `P1-6` Backfill + matching fallback for `trace_lot_no`
  - Scope:
    - Backfill historical P2 items and enable trace/artifact resolver to prefer `trace_lot_no`.
  - Deliverables:
    - Backfill script updates `trace_lot_no` from (`lot_no_norm`, `winder_number`).
    - Resolver/mapping path supports `trace_lot_no` first, then old fallback.
    - Diagnostic output reports hit stage (`matched_trace_lot` / fallback).
  - Acceptance:
    - `TC-P1-11`: Historical rows are backfilled with non-empty `trace_lot_no` when source fields exist.
    - `TC-P1-12`: Complaint product_id to artifact matching succeeds via `trace_lot_no` for 7+2+2 keys.
    - `TC-P1-13`: Partial unmatched case still returns deterministic reason code and no 500.
  - Progress (2026-03-08):
    - Done: backfill script added (`scripts/manual/backfill_p2_trace_lot_no.py`) and executed once (`scanned=280 updated=180 skipped=0` in demo DB).
    - Done: `resolve-input` now checks DB `trace_lot_no` tokens first, then falls back to `traceability`.
    - Done: diagnostics include `trace_source` (`db_trace_lot` / `traceability`).
    - Done: API test added for DB-first token resolution path and passing.
    - Done: complaint-analysis mapping stage adds `matched_trace_lot` when `trace_source=db_trace_lot`.
  - Remaining test records:
    - Done: `TC-P1-11` backfill verification on demo DB:
      - `audit_p2_trace_lot_no_backfill`: `scanned=280 already_filled=280 fillable=0`
      - `backfill_p2_trace_lot_no`: `scanned=280 updated=0 skipped=0`
      - Conclusion: historical rows are already fully backfilled (no pending fillable rows).
    - Attempted but not passed: `TC-P1-12` rerun (2026-03-08) with 7-file re-import (`140` rows, all jobs `COMPLETED`), but complaint set `resolved_count` stayed `2 -> 2` (no uplift).
      - Requested path `新侑特資料/P2/all` not found in runtime; used 7 files under `新侑特資料/P2`.
      - Evidence appended in `SMOKE_TEST_DEMO_RESULT_20260223.md` section `TC-P1-12 Rerun`.
      - Added audit script `scripts/manual/audit_artifact_token_intersection.py` and output `dev-guides/tc_p1_12_audit_result_20260308.json`:
        - `recommended_product_ids_by_trace_intersection = []`
        - `guaranteed_product_ids_by_matches = [20250909_P23_238-4_301]`
        - guaranteed-set rerun gives `requested=1 resolved=2 unmatched=0`.
    - Partially done: `TC-P1-13` API-level partial unmatched evidence has been backfilled into `SMOKE_TEST_DEMO_RESULT_20260223.md`.
      - Passing automated API coverage:
        - `test_v2_analytics_complaint_analysis_partial_hit_keeps_analysis_and_diagnostics`
        - `test_v2_analytics_artifacts_resolve_input_prefers_db_trace_lot_tokens`
      - Pending: one UI end-to-end screenshot/evidence entry from demo page interaction.
  - Follow-up solutions:
    - Done: added audit script `scripts/manual/audit_p2_trace_lot_no_backfill.py` to classify `updated=0` causes:
      - `already_filled`
      - `fillable`
      - `missing_lot_no_raw`
      - `invalid_lot_no_raw_format`
      - `missing_winder_number`
      - `invalid_winder_number`
      - Run command:
        - `cd form-analysis-server/backend`
        - `python -m scripts.manual.audit_p2_trace_lot_no_backfill`
    - Add one deterministic fixture import (small 3-file P1/P2/P3 set) for CI to validate `matched_trace_lot` stage.
    - Next hardening for TC-P1-12:
      - Ensure P3 source-winder chain is populated (currently demo DB `p3_items_v2.source_winder` is null), then rerun trace-intersection audit.
    - After re-import, rerun `resolve-input` and `complaint-analysis` API smoke and append request/response snippets into smoke result doc.


- [X] TODO：加入demo/開發 分離環境
驗證：可同時執行demo與開發的image，且更新開發用image時，demo image可正常使用

- [X] FIX：轉檔失敗原因：PdfServerNotConfigured: PDF_SERVER_URL is not configured
驗證：使用PDF上傳時，可以正常轉換資料
修復內容：
  - 新增 `GET /api/upload/pdf/service-status` 端點，讓前端可檢查 PDF 服務是否可用
  - `POST /api/upload/pdf/{process_id}/convert` 端點現在會在觸發轉檔前檢查 PDF_SERVER_URL
  - 若未配置，回傳 HTTP 503 Service Unavailable 並提供明確錯誤訊息
配置方式：在 `.env.demo` 或 `.env.dev` 中設定 `PDF_SERVER_URL=http://your-pdf-server/process`

- [ ] UX：專有名詞進行i18n轉換
驗證：在traditional chinese模式下，p1、p2、p3表格的title使用中文進行顯示；改善報告也具備翻譯內容

- [ ] FEATURE：經由資料分析 -> 客訴輸入的product_id標記為客訴內容
驗證：在資料查詢中，可以正確篩選出對應的客訴資料內容(P1+P2+P3)