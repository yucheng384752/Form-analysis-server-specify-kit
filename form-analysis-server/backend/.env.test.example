# Backend test template. Copy to .env.test locally if needed.
ENVIRONMENT=testing
AUTH_MODE=off
ALLOW_AUTH_OFF=true
SECRET_KEY=replace-with-test-only-secret-at-least-32-characters
DATABASE_URL=sqlite+aiosqlite:///./test.db

